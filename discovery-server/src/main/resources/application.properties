# Run discovery server on port 8761
server.port=8761

# Service name
spring.application.name=discovery-server

# Eureka settings - since this is the server, it doesn?t register itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
